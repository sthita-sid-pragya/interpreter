# interpreter
Programming language interpreter using JS.

Implements its own syntactic sugar,




__tests__
    Includes tests for:
        -math-expression
        -if-statement
        -for/while loops
        -built-in function
        -lambda-function
        -switch cases

